//import {} from '../actions/recoAction';

const INITIAL_STATE={
    phoneCode: '',
    phonePattern: ''
};

export default function (state=INITIAL_STATE, action)
{
    switch(action.type)
    {
        default:
            return state;
    }
}

